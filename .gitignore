# local observation store — regenerable, grows with every run
/data/

# node
node_modules/
*.log

# os
.DS_Store

# published output — generated by export.mjs, force-pushed to the `data` branch by CI.
# Not committed to main: it would churn on every run for no benefit to anyone reading the code.
/public/
